# 实施路线

当前：M0 文档与示例已写入；M1–M4 尚未实现。复选框表示功能实际完成情况。

## M0：设计基线

- [x] 定义通用框架与独立仓库边界。
- [x] 定义 Codex 交互接入和未来非交互接入方式。
- [x] 编写任务、证据、状态与操作语义草案。
- [x] 记录研究依据、自改进评价方法和实现待决问题。
- [x] 提供未接入真实仓库的项目样例。

## M1：可恢复的最小核心

- [ ] 选择运行环境和持久化实现；形成正式 schema 与迁移规则。
- [ ] 实现幂等项目接入，保留原有 AGENTS.md 和用户配置。
- [ ] 实现 context、start_run、run_status、record_evidence、record_decision、handoff。
- [ ] 实现本地 runner、事务状态更新、幂等启动与基本预算检查。
- [ ] 实现状态导出/恢复，明确产物备份责任。
- [ ] 支持项目内 Codex 调用，生成最小且版本固定的上下文。

验收：小型 CPU 任务可启动、完成、关联真实产物；中断 Agent 会话后可查询同一作业；重复请求不重复启动；缺失产物不能被标记为验证成功；第二次接入不损坏已有文件。机器重启能力按 backend 实际声明验收。

## M2：验证通用接入

- [ ] 接入一个小型离线 ML 实验和一个独立算法优化任务。
- [ ] 增加第二个任务主要修改适配器和配置，不修改通用核心。
- [ ] 记录代码、数据、配置和评价器版本；独立重跑核对结果。
- [ ] 验证两个任务的状态、经验与产物互不串用。

ACT 可作为后续真实项目，不要求第一版下载其数据或连接机器人。示例不能作为 ACT 已接入证据。

## M3：自动 Agent 适配与经验

- [ ] 实现 Codex 非交互适配、结构化输出检查和能力协商。
- [ ] 将长作业生命周期与 Agent 会话解耦。
- [ ] 实现经验来源、适用范围、反例与失效记录。
- [ ] 比较有无经验时的完成质量、成本和介入次数。
- [ ] 按实际需要增加 MCP 或远程 runner，共用核心操作。

验收：替换会话可恢复；Agent 退出成功但产物错误仍不能通过验证；经验只在匹配范围使用。

## M4：受评估的系统改进

- [ ] 实现候选分支、父版本、预算和比较记录。
- [ ] 建立开发/保留任务划分及独立评价权限。
- [ ] 进行重复运行、顺序扰动与跨任务验证。
- [ ] 实现采用与回退，区分模型升级和策略改进收益。

验收：候选不能更改其评分条件；改进具有可核查证据及不确定性说明；未通过候选不替换稳定版本。

## 下一项可执行开发任务

实现 M1 的“项目接入 + context”纵向切片：以临时研究仓库验证原有指令保留、本机路径解析、缺失配置提示、协议版本检查和重复接入。完成后再接入 start_run，避免先造完整调度平台。
