# 决策记录

日期：2026-09-07。依据与用户的架构讨论，以下方向已确认。

| ID | 决策 | 原因 |
|---|---|---|
| D001 | researchOS 是可接入新任务的通用框架 | 不把 ACT 的流程固化为核心 |
| D002 | 框架、研究项目、硬件运行时分别维护 | 独立演进，避免复制研究代码 |
| D003 | 首先支持用户在项目中直接使用 Codex | 匹配当前工作方式 |
| D004 | CLI 操作优先，后续增加系统启动 Agent 和 MCP | 共用核心协议，按需增加传输层 |
| D005 | 模型、提示、Agent 分工和研究策略可替换 | 为更强模型和更好的组织方式留空间 |
| D006 | 证据和研究状态独立于聊天 | 支持新会话接手及模型替换 |
| D007 | 系统改进单独评估、版本化采用 | 避免不明来源的变化影响研究 |
| D008 | 当前交付设计文档与示例 | 尚不将拟议操作宣传为可用产品 |

## 已授权的自主范围设计

用户确定研究目标、资源边界和重大方向调整。在已约定范围内，程序修复、实验执行、分析与约定的重复实验自主推进。是否需要进一步决定取决于任务已有授权；不要对每条可逆命令重复请求确认。

## 实现前待确定

| 问题 | 默认倾向 | 何时决定 |
|---|---|---|
| 核心语言与持久化 | Python 与 SQLite 可作为首版候选，尚未选定 | M1 实现时依据环境决定 |
| 首个 runner | 本地 CPU 作业，随后扩展 GPU/远程 | M1 |
| 第二个接入任务 | 与 ML 任务不同的轻量算法优化任务 | M2 |
| 备份与跨机器同步 | 明确导出状态、同步产物；先避免多主写入 | M1/M2 |
| 自动采用策略的门槛 | 初期输出候选和比较报告，按项目授权采用 | M4 |
| 真机接入 | 后续独立适配；现场监督和复位条件由具体项目定义 | 对应项目接入时 |

这些是实施选择，不要求用户现在逐一决定。后续 Agent 应根据可验证环境处理常规选择并记录原因。
